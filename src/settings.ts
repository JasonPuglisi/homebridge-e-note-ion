/**
 * Platform name registered with Homebridge. Must match `pluginAlias` in
 * config.schema.json.
 */
export const PLATFORM_NAME = 'ENotEion';

/** The npm package name — must match the `name` field in package.json. */
export const PLUGIN_NAME = 'homebridge-e-note-ion';
